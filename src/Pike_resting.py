import Pike
import math

class PikeResting(Pike):
    def __init__(self, Pike):
        self.pos = Pike.pos
        self.vx = Pike.vx
        self.vy = Pike.vy
        self.ax = Pike.ax
        self.ay = Pike.ay

    def __move():


    def activity(self):
        '''пусть здесь будет происходить рождение'''

    def observe(self, entities):
        if abs(self.x - self.r) < Frame.width/16 :
            self.ax += (Vx**2)/(self.x-Frame.width)
        if abs()
        for entity in entities:
            if
